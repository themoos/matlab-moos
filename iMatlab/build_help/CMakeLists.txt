cmake_minimum_required(VERSION 2.8)
find_package(MOOS 10.0)
FOREACH (dir ${MOOS_INCLUDE_DIRS})
    MESSAGE("INCLUDE:"${dir})
ENDFOREACH(dir)

GET_TARGET_PROPERTY(MOOS_LIB_LOCATION MOOS IMPORTED_LOCATION_NOCONFIG)
MESSAGE("LIB:"${MOOS_LIB_LOCATION})